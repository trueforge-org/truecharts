{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . -}}

{{/* Render secrets for bookorbit */}}
{{- $secrets := include "bookorbit.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "bookorbit-secrets" $secrets -}}
{{- end -}}

{{/* Render the templates */}}
{{- include "tc.v1.common.loader.apply" . -}}
