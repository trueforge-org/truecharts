{{- include "tc.v1.common.loader.init" . }}

{{/* Render the root credentials Secret */}}
{{- $secrets := include "minio.secrets" . | fromYaml -}}
{{- if $secrets -}}
  {{- $_ := set .Values.secret "root" $secrets -}}
{{- end -}}

{{- include "tc.v1.common.loader.apply" . }}
