# yaml-language-server: $schema=./values.schema.json
image:
  repository: ghcr.io/minekube/gate
  pullPolicy: IfNotPresent
  tag: latest@sha256:a65748c923600b48d7a5fd3131f72f9693298b75539d3f973cb272c62216511b
service:
  main:
    enabled: true
    ports:
      main:
        enabled: true
        protocol: tcp
        port: 25565
      query:
        enabled: true
        protocol: udp
        port: 25565
workload:
  main:
    podSpec:
      containers:
        main:
          tty: true
          stdin: true
          probes:
            liveness:
              type: tcp
            readiness:
              type: tcp
            startup:
              type: tcp
# https://gate.minekube.com/guide/lite.html
# https://gate.minekube.com/guide/config/
configmap:
  config:
    enabled: true
    data:
      config.yml: |
        config:
          bind: 0.0.0.0:{{ .Values.service.main.ports.main.port }}
          query:
            enabled: true
            port: {{ .Values.service.main.ports.query.port }}
            showPlugins: false
          lite:
            enabled: true
            routes:
              - host: minecraft.local
                backend: localhost:25565
persistence:
  config-file:
    enabled: true
    type: configmap
    objectName: config
    mountPath: /config.yml
    subPath: config.yml
