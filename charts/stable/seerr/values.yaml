image:
  repository: oci.trueforge.org/containerforge/seerr
  tag: "2.7.3@sha256:53f8d26cce4a541f968d4475a39cea7cb691106e6f154c5836d745937899415a"
  pullPolicy: IfNotPresent
service:
  main:
    ports:
      main:
        port: 10241
persistence:
  config:
    enabled: true
    mountPath: "/app/config"

workload:
  main:
    podSpec:
      containers:
        main:
          env:
            PORT: "{{ .Values.service.main.ports.main.port }}"
            # User Defined
            JELLYFIN_TYPE: ""
            LOG_LEVEL: "info"
