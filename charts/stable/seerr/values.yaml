image:
  repository: oci.trueforge.org/containerforge/seerr
  tag: "2.7.3@sha256:9e0d621f5e982ad71e96569ec0166c214e4600afdf133a11079fd657a5dc6c6b"
  pullPolicy: IfNotPresent
service:
  main:
    ports:
      main:
        port: 10241
persistence:
  config:
    enabled: true
    mountPath: "/app/config"

workload:
  main:
    podSpec:
      containers:
        main:
          env:
            PORT: "{{ .Values.service.main.ports.main.port }}"
            # User Defined
            JELLYFIN_TYPE: ""
            LOG_LEVEL: "info"
