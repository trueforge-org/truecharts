{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "NVIDIA_CAPS": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Configuration for `containerOptions.NVIDIA_CAPS.$item`."
      },
      "description": "Defines the NVIDIA_CAPS to be passed as an environment variable to the container.",
      "default": "[\"all\"]"
    }
  },
  "additionalProperties": true,
  "description": "Options that apply to all containers, unless overridden at the container level See more info about containerOptions [here](/truecharts-common/containeroptions)"
}
