configMapsFromFolder:
  enabled: true
  basePath: "files/configMaps"
  configMapsOverrides:
    test-config:
      labels:
        test-label: "config-label"
      annotations:
        test-annotation: "config-annotation"
    test-scripts:
      labels:
        test-label: "scripts-label"
